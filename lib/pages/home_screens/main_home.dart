import 'package:flutter/material.dart';
class GoingOns extends StatelessWidget {
  const GoingOns({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('What is going on'));
  }
}
